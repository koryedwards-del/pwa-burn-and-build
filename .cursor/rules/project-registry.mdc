---
description: Project map and hosting context
alwaysApply: true
---

# Burn & Build — one repo, separate products

Repo: **`pwa-burn-and-build`** (this workspace)

**Keep top products separate.** Do not merge flows, copy, CTAs, or implementation between them unless the user explicitly asks.

**Product name:** **Menu planner** (folder/URL alias: `menuplanner/` → redirects). Never use “meal planner” in user-facing copy or docs.

---

## Product A — Desktop web app (active build)

**Site:** burnandbuilddiet.com

| Path | Role |
|------|------|
| `landing/` | Marketing → questionnaire |
| `questionnaire/` | Intake → save plan → checkout handoff |
| `createyourfoodplan/` | Plan-ready + Stripe paywall ($149 per program) |
| `program-report/` | **Unified program shell** — Welcome, Projections, Plan/Servings, Menu planner |
| `menuplanner/` | Redirect → `program-report/?page=menuplanner` |
| `menuplanner/js/planner.js` | Menu planner facade (state, views, print modules) |
| `mealplanner/` | Redirect stub → menu planner deep link |
| `server/` | API + SQLite — programs, per-program payment |

**Desktop flow:**
```
Landing → questionnaire → Stripe (per program) → program-report (pages 1–4)
```

**Return access:** `burnandbuilddiet.com/menuplanner/` or `program-report/?page=menuplanner` + email → unlock program.

- Each new program = its own Stripe checkout (`programId`).
- **End of desktop product** = Menu planner (page 4 inside program-report).

---

## Product B — Phone PWA (separate; do not conflate)

| Path | Role |
|------|------|
| `myplan/` | Phone daily app (PWA) |
| `js/shellApp.js` | Phone shell entry module |

Not part of the desktop funnel or menu planner. Legacy `js/app.js`, `js/shell.js`, and root `sw.js` removed.

---

## Other

- **Signal+** — separate product.
- **Deploy:** push `main` → `.github/workflows/deploy-landing.yml` → GitHub Pages.
