---
description: Project map and hosting context
alwaysApply: true
---

# Project context

- Full map: `~/operatingmanual/PROJECTS.md`

## Two repos — keep them separate

| Repo | Host | Role |
|------|------|------|
| **`burnandbuildlandingpage`** | gettheburnandbuildapp.com (GitHub Pages) | Marketing only — hero, features, calculator, testimonials, pricing, CTA |
| **`pwa-burn-and-build`** | Render (`/start/` creator) + GitHub Pages (`/shell/` app) | Program creator + daily shell app — no duplicate marketing homepage |

**Flow:** Landing CTA → `program-creator.onrender.com/start/` → email → intake → Stripe → app.

- This workspace repo: **`pwa-burn-and-build`** — vanilla HTML/JS, localStorage + SQLite on Render
- Landing clone may exist at `burnandbuildlandingpage/` (gitignored here) for local preview only
- **Not connected to Hardkor Diet** (`hardkordietlandingpage` / thehardkordiet.com)

## Reminders

- **~Jul 5–6, 2026:** Check **Enforce HTTPS** for Hardkor (`thehardkordiet.com`) in GitHub Pages settings — DNS is green; cert may be ready by then.
